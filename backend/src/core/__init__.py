"""Framework kernel (src.core).

Non-domain infrastructure layer:
- ``llm``       : centralized, lazy, provider-routed LLM factory.
- ``logConfig`` : uvicorn / app logging configuration.

This package is stable for framework users, and is considered "foundation".
"""
